<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<mapper namespace="participationForumMapper">

    <resultMap id="askAndAnswerBoardVO" type="com.green.Library.web.participationForum.vo.AskAndAnswerBoardVO">
        <id column="ASK_AND_ANSWER_BOARD_NUM" property="askAndAnswerBoardNum" />
        <result column="ASK_AND_ANSWER_BOARD_PASSWORD" property="askAndAnswerBoardPassword" />
        <result column="ASK_AND_ANSWER_BOARD_PUBLIC_OR_PRIVATE" property="askAndAnswerBoardPublicOrPrivate" />
        <result column="IS_ANSWER_BOARD" property="isAnswerBoard" />
        <result column="IF_ANSWER_BOARD_NUM" property="ifAnswerBoardNum"/>
        <result column="origin_order_num" property="originOrderNum" />
        <result column="ANSWER_ORDER_NUM" property="answerOrderNum" />
        <result column="BOARD_NUM" property="boardNum" />
    </resultMap>

<!--    &lt;!&ndash; 공지사항 등록&ndash;&gt;-->
<!--    <insert id="insertBoard">-->
<!--        INSERT INTO BOARD (-->
<!--            BOARD_TITLE-->
<!--            , CONTENT-->
<!--            , BOARD_DATE-->
<!--            , BOARD_TYPE-->
<!--            , USER_CODE-->
<!--            , BOARD_CNT-->
<!--        ) VALUES (-->
<!--            #{boardTitle}-->
<!--            , #{content}-->
<!--            , #{boardDate}-->
<!--            , #{boardType}-->
<!--            , #{userCode}-->
<!--            , #{boardCnt}-->
<!--        )-->
<!--    </insert>-->

    <!--  첨부파일 등록  -->
    <insert id="insertUploadFile">
        INSERT INTO ATTACHED_FILE(
            ORIGIN_FILE_NAME
            , ATTACHED_FILE_NAME
            , BOARD_NO
        )
        VALUES
        <if test="fileList != null">
            <foreach collection="fileList" item="file" separator=",">
                (#{file.originFileName}
                , #{file.attachedFileName}
                , #{file.boardNo})
            </foreach>
        </if>

    </insert>

    <!-- 공지사항 조회-->
    <select id="selectNotice" resultMap="boardMapper.board">
        SELECT BOARD.BOARD_NO
            , BOARD_TITLE
            , CONTENT
            , BOARD_DATE
            , BOARD.USER_CODE
            , USERS.USER_NAME
            , BOARD_CNT
        FROM BOARD INNER JOIN USERS
        ON BOARD.USER_CODE = USERS.USER_CODE
        WHERE BOARD_TYPE = 31
        ORDER BY BOARD_NO DESC
    </select>


    <!-- 공지사항 상세조회-->
    <select id="noticeDetail" resultMap="boardMapper.board">
            SELECT BOARD.BOARD_NO
            , BOARD_TITLE
            , BOARD_DATE
            , BOARD_CNT
            , CONTENT
            , ATTACHED_FILE_NAME
            FROM BOARD INNER JOIN ATTACHED_FILE
            ON BOARD.BOARD_NO = ATTACHED_FILE.BOARD_NO
            WHERE BOARD.BOARD_NO = #{boardNo}
    </select>


    <!-- 다음에 insert 할 Code-->
    <select id="selectNextBoardCode" resultType="int">
        SELECT IFNULL(MAX(BOARD_NO), 0) + 1 FROM BOARD
    </select>

    <!--  등록한 게시물 정보 리스트  -->
    <select id="forumSelectBoardList" resultMap="boardMapper.board">
        SELECT
        BOARD.BOARD_NO
        , BOARD_TITLE
        , CONTENT
        , BOARD_DATE
        , BOARD.USER_CODE
        , BOARD_CNT
        , USER_ID
        , USER_NAME
        FROM board INNER JOIN USERS
        ON BOARD.USER_CODE = USERS.USER_CODE
        WHERE BOARD_TYPE = '31'
        <if test='searchValue != null and !searchValue.equals("")'>
            and ${searchType} LIKE CONCAT('%', #{searchValue} , '%')
        </if>
        ORDER BY BOARD_NO DESC
        LIMIT ${displayDataCnt} OFFSET ${displayDataCnt * (nowPage - 1)}
    </select>

    <!--@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ 묻고답하기 @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@-->

<!--    OrderBy를 이용하기 위해 넣은 컬럼 -->
<!--    질문글이면 1씩 증가 시켜야하고 답변글이면 질문글에 대한 번호를 가져야함-->
    <select id="selectMaxOriginOrderNum" resultType="int">
        SELECT
            CASE
                when MAX(origin_order_num) IS null then 1
                ELSE MAX(origin_order_num)+1
            END
        FROM
        ask_and_answer_board;
    </select>

    <!--    OrderBy를 이용하기 위해 넣은 컬럼 -->
<!--   해당 질문글의 order_num 으로 검색해서 answer_order_num이 0인 경우는 맨처음 답글을 단것 -->
<!--    0이 아닌 경우에는 해당 질문글에 여러개의 답변이 달린것 -->
    <select id="chkAnswerOrderNum" resultType="int">
        select
            case
                when max(ANSWER_ORDER_NUM) is null then 0
                else max(ANSWER_ORDER_NUM)
            end
        from ask_and_answer_board
        where origin_order_num = ${originOrderNum}
    </select>

<!--    답변을 하려는데 답변을 하고자하는 글의 origin_order_num를 구하는 쿼리문-->
    <select id="searchOriginOrderNumForAnswer" resultType="int">
        SELECT origin_order_num
        FROM ask_and_answer_board
        WHERE board_num = ${boardNum};
    </select>

<!--    질문글 인경우-->
    <insert id="insertAskAndAnswerBoard">
        insert into ASK_AND_ANSWER_BOARD (
        ASK_AND_ANSWER_BOARD_PASSWORD ,
        ASK_AND_ANSWER_BOARD_PUBLIC_OR_PRIVATE ,
        origin_order_num ,
        ANSWER_ORDER_NUM ,
        BOARD_NUM
        )
        VALUES(
        #{askAndAnswerBoardVO.askAndAnswerBoardPassword} ,
        #{askAndAnswerBoardVO.askAndAnswerBoardPublicOrPrivate},
        #{askAndAnswerBoardVO.originOrderNum} ,
        #{askAndAnswerBoardVO.answerOrderNum} ,
        #{boardNum}
        )
    </insert>

<!--    답변글 인경우-->
    <insert id="insertAskAndAnswerBoard2">
        insert into ASK_AND_ANSWER_BOARD (
        ASK_AND_ANSWER_BOARD_PASSWORD ,
        ASK_AND_ANSWER_BOARD_PUBLIC_OR_PRIVATE ,
        BOARD_NUM,
        origin_order_num ,
        ANSWER_ORDER_NUM ,
        is_answer_board ,
        if_answer_board_num
        )
        VALUES(
        #{askAndAnswerBoardVO.askAndAnswerBoardPassword} ,
        #{askAndAnswerBoardVO.askAndAnswerBoardPublicOrPrivate},
        #{boardNum} ,
        #{askAndAnswerBoardVO.originOrderNum} ,
        #{askAndAnswerBoardVO.answerOrderNum} ,
        #{askAndAnswerBoardVO.isAnswerBoard} ,
        #{askAndAnswerBoardVO.ifAnswerBoardNum}
        )
    </insert>

    <!--  파일 등록  -->
    <insert id="insertFileList">
        INSERT INTO ATTACHED_FILE(
        ORIGIN_FILE_NAME
        ,ATTACHED_FILE_NAME
        ,IS_MAIN
        ,BOARD_NUM
        )
        VALUES
        <foreach collection="fileList" item="file" separator=",">
            (#{file.originFileName}
            ,#{file.attachedFileName}
            ,#{file.isMain}
            ,#{file.boardNum})
        </foreach>
    </insert>
<!--게시판 등록-->
    <insert id="insertBoard">
        INSERT INTO BOARD(
        BOARD_TITLE
        , CONTENT
        , BOARD_TYPE
        , USER_CODE
        , BOARD_CNT
        , BOARD_NUM
        )
        VALUES(
        #{boardTitle}
        ,#{content}
        ,#{boardType}
        ,#{userCode}
        ,#{boardCnt}
        ,#{boardNum}
        )
    </insert>

    <select id="selectAskAndAnswerBoardList" resultMap="boardMapper.board">
        SELECT
            BOARD.BOARD_NUM ,
            BOARD.BOARD_TITLE ,
            DATE_FORMAT(BOARD.BOARD_DATE, '%Y-%m-%d') AS BOARD_DATE ,
            USERS.USER_NAME ,
            BOARD.BOARD_CNT ,
            ASK_AND_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_NUM ,
            ASK_AND_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_PUBLIC_OR_PRIVATE ,
            ASK_AND_ANSWER_BOARD.IS_ANSWER_BOARD ,
            ASK_AND_ANSWER_BOARD.IF_ANSWER_BOARD_NUM ,
            ask_and_answer_board.ORIGIN_ORDER_NUM ,
            ask_and_answer_board.ANSWER_ORDER_NUM
        FROM
            BOARD INNER JOIN users
            ON board.USER_CODE = users.USER_CODE
            INNER JOIN ASK_AND_ANSWER_BOARD
            ON board.BOARD_NUM = ASK_AND_ANSWER_BOARD.BOARD_NUM
        WHERE
            board_type = 30
        <if test='searchValue != null and !searchValue.equals("")'>
            and ${searchType} LIKE CONCAT('%', #{searchValue} , '%')
        </if>
        ORDER BY ask_and_answer_board.ORIGIN_ORDER_NUM DESC , ask_and_answer_board.ANSWER_ORDER_NUM ASC
        LIMIT ${displayDataCnt} OFFSET ${displayDataCnt * (nowPage - 1)}
    </select>


    <!--  게시판 개수 조회 -->
    <select id="partiCountBoard" resultType="int">
    SELECT
        COUNT (BOARD_NUM)
    FROM
        BOARD
    WHERE
        board_type = 30
    <if test='searchValue != null and !searchValue.equals("")'>
        AND ${searchType} LIKE CONCAT('%', #{searchValue} , '%')
    </if>
    </select>

    <select id="detailAskBoard" resultMap="boardMapper.board">
        SELECT
            board.BOARD_NUM ,
            board.BOARD_TITLE ,
            CONTENT ,
            board.BOARD_DATE ,
            board.USER_CODE ,
            board.BOARD_CNT ,
            users.USER_CODE ,
            users.USER_ID ,
            users.IS_ADMIN ,
            attached_file.ORIGIN_FILE_NAME ,
            attached_file.ATTACHED_FILE_NAME ,
            ASK_and_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_NUM ,
            ASK_AND_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_PASSWORD ,
            ASK_AND_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_PUBLIC_OR_PRIVATE ,
            ASK_AND_ANSWER_BOARD.IS_ANSWER_BOARD ,
            ASK_AND_ANSWER_BOARD.IF_ANSWER_BOARD_NUM ,
            ASK_AND_ANSWER_BOARD.ORIGIN_ORDER_NUM ,
            ASK_AND_ANSWER_BOARD.ANSWER_ORDER_NUM
        FROM
            BOARD INNER JOIN attached_file
            ON BOARD.BOARD_NUM = attached_file.BOARD_NUM
            INNER JOIN ASK_AND_ANSWER_BOARD
            ON BOARD.BOARD_NUM = ASK_AND_ANSWER_BOARD.BOARD_NUM
            INNER JOIN users
            ON BOARD.USER_CODE = users.user_code
        WHERE
            BOARD.BOARD_NUM = ${boardNum}

    </select>

    <!--  조회수 증가  -->
    <update id="boardCntUp">
        UPDATE BOARD
        SET BOARD_CNT = BOARD_CNT + 1
        WHERE BOARD_NUM = ${boardNum}
    </update>

<!--    이건 답변 글 검색-->
    <select id="selectAnswerBoardList" resultMap="boardMapper.board">
        SELECT
        BOARD.BOARD_NUM ,
        BOARD.BOARD_TITLE ,
        DATE_FORMAT(BOARD.BOARD_DATE, '%Y-%m-%d') AS BOARD_DATE ,
        USERS.USER_NAME ,
        BOARD.BOARD_CNT ,
        ASK_AND_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_NUM ,
        ASK_AND_ANSWER_BOARD.ASK_AND_ANSWER_BOARD_PUBLIC_OR_PRIVATE ,
        ASK_AND_ANSWER_BOARD.IS_ANSWER_BOARD ,
        ASK_AND_ANSWER_BOARD.IF_ANSWER_BOARD_NUM
        FROM
        BOARD INNER JOIN users
        ON board.USER_CODE = users.USER_CODE
        INNER JOIN ASK_AND_ANSWER_BOARD
        ON board.BOARD_NUM = ASK_AND_ANSWER_BOARD.BOARD_NUM
        WHERE
        board_type = 30 AND
        is_answer_board = 'Y'
        <if test='searchValue != null and !searchValue.equals("")'>
            and ${searchType} LIKE CONCAT('%', #{searchValue} , '%')
        </if>
        ORDER BY BOARD_NUM DESC
        LIMIT ${displayDataCnt} OFFSET ${displayDataCnt * (nowPage - 1)}
    </select>
    
<!--    질문응답 게시물 삭제 1-->
    <delete id="deleteBoard">
        delete from board
        where board_num = #{boardNum}
    </delete>
<!--만약 질문글을 삭제할경우 답변글도 다같이 삭제해야함..-->
    <select id="deleteAskBoard1" resultType="list">
        select
            board.board_num
        from
            board inner join ASK_AND_ANSWER_BOARD
            on board.board_num = ASK_AND_ANSWER_BOARD.board_num
        where
        ASK_AND_ANSWER_BOARD.ORIGIN_ORDER_NUM = #{originOrderNum}
    </select>


</mapper>






























